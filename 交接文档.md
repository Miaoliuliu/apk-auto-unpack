# 交接文档

> 生成时间：2026-09-07 11:28（本地）
> 项目：D:\PythonProject\apk-auto-unpack
> 来源会话：结束会话自动交接

## 1. 本次会话做了什么

围绕 **通联地址提取**（静态规则 + 运行时网络 hook 方案），不碰壳识别/脱壳主线。

- **评审** `src/auto_unpack/extraction/`：静态提取可行、完整性不够（混淆/拼接/加密看不到）；画布在 Cursor canvases：`extraction-code-review.canvas.tsx`。
- **修 P0（已合进代码）**：IPv6 不再因「host 无点号」被打成 noise；短域名误杀收紧（放行 `m.`/`s.`/`i.` 等与 `jd.com` 类 2 字符 SLD）。
- **定提取架构（未写进 pipeline）**：无壳 = 提取阶段静态 + Frida 网络 hook（此时才 install）；有壳 = dump DEX 静态 + 提取阶段再 hook（脱壳阶段已 install）；**卸载统一在全流程结束**。
- **试跑脚本** `src/auto_unpack/extraction/net_hook.js`（未接入 `run.py` / `pipeline.py`）。
- **真机试跑**：撸管神器（OkHttp 完整 URL 成功）；`hbksx.mgdgpl.etxpo`（只有 `libc.connect` IP，随后进程退出）。

## 2. 主要决策

| 决策 | 原因 | 影响 |
|------|------|------|
| P0 先修误杀再谈解码层 | IPv6/短域名是「抽到了却丢掉」 | `indicators.py` 用 `ipaddress`；`_IP_RE` 已删除 |
| 无壳主路径仍是静态；Frida 对无壳可选 | 无壳默认不 spawn，强上设备会拖垮批量 | 设计：静态必跑，hook 失败不整单失败 |
| 有壳 Frida 两次可接受（dump 一次 + 提取 hook 一次） | 层清晰，比把 dump 会话硬留到提取更好控 | 以后可把 hook 塞进 dump spawn，对外合同不变 |
| 网络 hook 放在**提取层**，不放脱壳 flow | 无壳也要 hook；有壳卸包不能早于提取 | **现状冲突**：`_maybe_uninstall` 仍在脱壳 `finally` |
| 不把 P1 转义/子串 base64 当主方案 | 可逆藏法只是补洞；能用的地址靠 dump 完整或运行时 | 解码层仍 backlog |
| `etxpo` 类结果标 IP 通联，不当完整 URL | native `connect` 无 path；Java OkHttp3 未挂上 | 接入流水线时要分 `runtime` / `runtime_ip` |

## 3. 代码 / 产物变更要点

- `src/auto_unpack/extraction/indicators.py`
  - `_parse_ip` / `_format_ip_host`；IPv6 方括号提取（文本 + 二进制）；`CLEAN_RE` 不再剥 IPv6 的 `]`
  - `is_noise_url`：IP 用 loopback/unspecified/link-local/公共 DNS（含 IPv6）/DNS 端口，**不再** `"." not in host → noise`
  - `_looks_plausible_url`：单字母子域白名单 `msivwtu`；去掉「非 TLD 标签全 ≤2」一刀切
- `tests/test_extraction_indicators.py`：IPv6 / 短域名 / 字母紧贴 `[ipv6]` 回归
- `src/auto_unpack/extraction/net_hook.js`：**仅试跑**。挂 Java URL/URI/HttpURLConnection、OkHttp3 与 **AOSP** `com.android.okhttp`、WebView、Socket、`libc.connect`。`console.log` + `send()` 导致 Frida CLI 每条打两遍。
- **未改**：`pipeline.py` 卸载时机、提取层 spawn、hook 结果 merge 进 `task.urls`

行为变化（用户可感知，仅静态规则）：

- `http://[2001:db8::1]:8080/api` 可进 biz；`::1` / 公共 DNS IPv6 仍是 noise
- `https://m.myservice.com/v1`、`https://jd.com/login` 不再被判假 host

## 4. 当前状态

- **能用 / 已验证**
  - 生产主路径不变：`run.py` → 识别壳 → dpt 才 Frida dump → **静态**抽 URL；无壳只静态。
  - P0 测试：`workbuddy python -m pytest tests/test_extraction_*` 当时全绿。
  - 试跑 撸管神器：包名 **`shop.ymgakelc.joesh33zka`**（APK 静态启发式误报 `com.openinstall.APP_KEY`，不可信）。OkHttp 打到 `https://luguan3.hbchcyy.cn/api/getdata/gettypeproducts?...`；大量 `/upload/*.js` 是 CDN，友盟 `*.umeng.com` 是 SDK。
  - 试跑 **`hbksx.mgdgpl.etxpo`**：只有 `libc.connect`；`:9527` 一批 GCP/AWS IP 更像业务长连接，`:443` 偏 CDN；无 `okhttp3` hook 日志；`Process terminated`。
- **已知限制或坑**
  - 畸形 Manifest **禁止**用启发式包名 spawn；信 `--package` / `adb install` 后的包名。撸管神器即反例。
  - `net_hook.js` 未枚举加固后的 OkHttp3 类名；Flutter/自研 so 只能看到 IP。
  - 有壳 `_maybe_uninstall` 在脱壳结束就卸（`pipeline.py`），提取层再 hook 会没 App。
  - 不设 `PYTHONUTF8=1`；editable + 中文路径会崩，项目已在 `D:\PythonProject\apk-auto-unpack`。
  - PortableGit：偶发 `git commit` reflog 成功但 ref 不落盘。
- **未完成 / 待办**
  1. 卸载挪到 `run()` 全流程收尾；无壳在提取阶段 `maybe_install`。
  2. 提取层加载 `net_hook.js`，`send` 的 URL merge 进 `merge_indicators`（`source=runtime`）；hook 失败不否决静态 biz。
  3. 脚本去重：同一 URL 只留一层；CLI 不要 `send`+`console.log` 双打；`/upload/*.js`、友盟进 noise。
  4. native 样本：延后 resume / attach 防秒退；补 `SSL_write`/`send`；枚举 `okhttp3.*`。
  5. P1 静态解码（`\u0068ttp`、子串 base64）仍 backlog，优先级低于 hook 接入。
  6. 更早主线 packer（Virbox / 复合壳标签）本次未动。

## 5. 新会话如何继续

按优先级（用户若未改口，通联线优先于 packer）：

1. **把卸载从脱壳 `finally` 挪到全流程结束**，否则有壳提取 hook 无法落地。
2. **提取层接入 `net_hook.js`**：静态先跑，再 spawn 挂钩；结果按 URL 去重后写入 `task.urls`。
3. 再处理 `etxpo` 类 native（秒退 + 无 Java URL）。

建议开场（可复制）：

```
请先阅读仓库根目录 交接文档.md，然后继续：把 pipeline 里 _maybe_uninstall 从脱壳 finally 挪到全流程结束，并在提取层接入 net_hook.js（静态先跑，hook 失败不否决已有 biz URL）。
```

若改回壳识别主线：Virbox 特征进 `packer_sigs` + 复合壳结构化标签（`绘本.apk` 360+dpt+自定义）。

## 6. 关键命令 / 入口

```bash
cd D:\PythonProject\apk-auto-unpack

# 全流程（PyCharm / 系统 Python 3.10 + frida）
python run.py

# 静态规则测试
C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe -m pytest tests/test_extraction_indicators.py -q

# 试跑网络 hook（包须已安装；畸形包名用 adb 查）
adb shell pm list packages -3
frida -U -f shop.ymgakelc.joesh33zka -l src/auto_unpack/extraction/net_hook.js --no-pause
frida -U -f hbksx.mgdgpl.etxpo -l src/auto_unpack/extraction/net_hook.js
```

样本：`outputs/packer_detection/无壳/撸管神器.apk` → 设备包名 `shop.ymgakelc.joesh33zka`。
布局：`src/auto_unpack/{flow,runtime,packer,extraction,unpacker}/`；规则在 `extraction/indicators.py`，扫描在 `analyze.py`，试跑 hook 在 `extraction/net_hook.js`。
