# 交接文档

> 生成时间：2026-09-10 09:15（本地）
> 项目：D:\PythonProject\apk-auto-unpack
> 来源会话：结束会话自动交接

## 1. 本次会话做了什么

通联提取线：对照 9-09 产物收紧静态规则，**不碰壳识别/脱壳，不接 `net_hook.js`**。

- 读旧交接后看 `outputs/extracted_urls`：上午旧规则产物 + 下午 14:56–15:27 新跑约 20 个。CRL/DoH/oceanengine 进 biz 已压住；仍脏的是 CIDR、`geosite.dat`、JS 粘在 IP 上。
- **已改代码（两轮）**：
  1. 三层闸：语法闸（包名/so 路径不是 URL）；角色闸（`.crl`/`/dns-query`/NLS、SDK so 来源）；拿掉 `/api/` `/v2/` `/login` 单独抬 biz。漏网短名单：weibo/oceanengine/sigmob/hicloud 等。
  2. 用户确认后再改三刀：CIDR 不当 URL；跳过 `geosite.dat`/`geoip.dat`；IP 路径含 `)`/`,` 或 `/i` 当 JS 粘连。
- 抽查过的真 C2 仍在：`api.botgate.cn`、`api.lmac.cc`、`39.108.101.79:55007`、密空间 `166.75.114.139/api0703.json` 等。
- 写了提交前审查 skill（个人 + 本仓库 `.cursor/skills/pre-commit-review`）。提交前必须真跑测试并出【审查报告】。

## 2. 主要决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 通联 = 样本自身业务出口，不是「像 URL 的字符串」 | `/api/` 把广告/云 SDK 抬成 biz | `url_rank` 不再用路径 hint 单独抬 biz，只留 `.php` / 灰产 TLD+scheme / `api.` 主机 / IP+非常用端口 / ws |
| 语法与可达性分开 | 用户要求死域名也要格式正确 | `validation.dns/http` 默认不测、不丢弃；语法不过关直接不进清单 |
| 形态过滤优先于域名名单 | 追 SDK 域名会无限循环 | CRL/DoH/NLS/CIDR/JS 粘连/geosite 用规则；weibo 等只当漏网短名单 |
| `net_hook.js` 仍不进自动化 | 上轮已否决；灰产 Frida 检测 | 不要 merge `source=runtime`，不要为 hook 挪 `_maybe_uninstall` |
| 提交前审查 skill 自动触发 | 禁止口头「看起来没问题」就 commit | 范围=本次 diff，不是整仓审计 |

仍有效（上轮）：畸形 Manifest 禁止启发式包名 spawn；IPv6/短域名 P0；`_maybe_uninstall` 仍在脱壳 `finally`。

## 3. 代码 / 产物变更要点

- `src/auto_unpack/extraction/indicators.py`：`_is_well_formed_locator`、CIDR、`_is_infra_shape`、`_trim_binary_glue`、SDK 来源降 noise、Go 长度前缀 `0github.com`
- `src/auto_unpack/extraction/analyze.py`：`_SKIP_LIST_FILES` 增加 geosite/geoip
- `tests/test_extraction_indicators.py`、`tests/test_extraction_outputs.py`
- `.cursor/skills/pre-commit-review/SKILL.md`（个人副本在 `C:\Users\Administrator\.cursor\skills\pre-commit-review\`）

行为变化（需用**当前**规则重跑才完全反映到下午那批）：

- `10.0.0.0/8`、`1.2.3.4/i),a=null...` → 不是候选
- `geosite.dat` 不再扫描（coterie 曾 24 万裸域名）
- `https://jd.com/login`、`www.*.com/api/` → weak，不是 biz
- 证书 CRL、DoH `/dns-query`、`nls-*.aliyuncs.com` → noise

**产物**：`outputs/extracted_urls/` 下午批次是三层闸之后、CIDR/geosite 三刀**之前**的；三刀后未再跑 APK。

## 4. 当前状态

- **能用 / 已验证**
  - 生产：`run.py` → 识别壳 → dpt 才 Frida dump → 静态抽 URL。
  - `pytest tests`：2026-09-10 提交前 353 项，exit 0。
- **已知限制或坑**
  - 自动化拿不到：加密 native、登录后下发的接口、Frida 检测包的运行时流量。
  - weak 仍可能有 Flutter 插件通道（`com.fluttercandies/...`）、个推 `:5224`；不淹没 biz，先不追。
  - 不设 `PYTHONUTF8=1`；解释器见第 6 节。
  - PortableGit：偶发 commit reflog 成功但 ref 不落盘。
- **未完成 / 待办**
  1. **用含 CIDR/geosite 三刀的规则重跑** 下午那批（至少 Anycast VPN、coterie、川川云手机），确认 biz 不再有网段和 `1.2.3.4/i)`。
  2. P1 静态解码（`\u0068ttp`、子串 base64）backlog。
  3. 壳识别：Virbox、复合壳（`绘本.apk` 360+dpt+自定义）。
  4. ~~net_hook 进 pipeline~~ **已否决**。

## 5. 新会话如何继续

按优先级：

1. 重跑下午样本，抽查 Anycast VPN / coterie / 川川云手机的 `urls_by_rank.txt`。
2. 仍脏再改形态规则，不要放宽无协议候选、不要把 `net_hook` 接回去。
3. 用户改口再碰壳识别。

建议开场（可复制）：

```
请先阅读仓库根目录 交接文档.md，然后继续：用当前规则重跑 Anycast VPN / coterie / 川川云手机，抽查 outputs/extracted_urls 的 biz 是否还含 CIDR 或 JS 粘连 IP。不要接入 net_hook.js。
```

## 6. 关键命令 / 入口

```bash
cd D:\PythonProject\apk-auto-unpack
python run.py

C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe -m pytest tests -q --tb=short
```

解释器：`C:/Program Files/Python310/python.exe`（frida + androguard）；pytest 用 workbuddy 环境。
布局：`src/auto_unpack/{flow,runtime,packer,extraction,unpacker}/`；规则 `extraction/indicators.py`，扫描 `extraction/analyze.py`。
`net_hook.js` 不要作为下一会话主线。
