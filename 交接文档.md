# 交接文档

> 生成时间：2026-09-08 14:09（本地）
> 项目：D:\PythonProject\apk-auto-unpack
> 来源会话：结束会话自动交接

## 1. 本次会话做了什么

通联提取线：先试运行时 Frida hook，**判定不可进自动化后放弃**；回头收紧静态规则。不碰壳识别/脱壳主线。

- 读旧交接后手工试跑 `net_hook.js`。用户用 `frida -H 127.0.0.1:14725 -f elSsGh.EAKYOI.NURZSj`：hook 生效，登录页只有 OpenInstall 完整 URL + `libc.connect` 的 `:8282` IP；进程随后退出。
- 讨论灰产（假贷款/约炮/色情）自动化：只能钩 DNS/`connect`/TLS 出口层，不能按网络框架适配。停在登录页只能抓启动通联，不是登录后 API。
- **改过** `src/auto_unpack/extraction/net_hook.js`（DNS/SNI/`SSL_write`/X5/降噪）。用户注入后 App **卡在加载页**（Frida 检测）。用户明确 **算了，不接 pipeline**。
- 评审 `outputs/extracted_urls`：
  - **9-07 产物**（约 21 个，旧格式）：明文 C2 能中（`yuliaotongxun.vip`、`xmrhmy.icu`、`api.botgate.cn`），但灰产 TLD / `/v2/` / `keyword.txt` 把 biz 搞脏。
  - **9-08 上午产物**（用户自己优化后新跑，约 30+）：无协议 host 全进清单，单文件可达数万行；biz 里大量 `*.cc` 源文件、`Api.java`、`Filled.Cloud`、`bad_domains.txt`。
- **已改代码（静态收口）**：无协议源码/图标丢掉；灰产 TLD 必须带 scheme 才 biz；`IP:非常用端口` 仍 biz；禁扫词表；so 无 scheme 不进 biz；`urls_by_rank.txt` 不再写 noise。测试当时全绿。

## 2. 主要决策

| 决策 | 原因 | 影响 |
|------|------|------|
| **运行时网络 hook 不进自动化** | 灰产 Frida 检测各异；注入即卡加载；对抗无法批量 | `net_hook.js` 仅残骸；**不要**再把卸载挪到全流程、不要 merge `source=runtime` |
| 通联主路径 = 高精度静态 | 明文 C2 多在 DEX/arsc/配置键里；昨天是「找到了分不清」 | 规则在 `indicators.py`，扫描在 `analyze.py` |
| 灰产 TLD 单独不能 biz | `.cc/.xyz/.click` 同时是源码后缀/图标常量 | 要 `http(s)://` 或 `host:非常用端口` 或业务路径 |
| 词表不当服务器 | 火鱼 `keyword.txt`、MT4 `bad_domains.txt` 灌爆 biz | `analyze.py` `_SKIP_LIST_FILES` |
| so 无 scheme 降权 | liteav/flutter/rustdesk 调试符号冒充 host | `make_indicator(..., "native")` 无端口则 cap 为 weak |
| P1 转义/子串 base64 仍非主方案 | 能用的地址靠明文或 dump 完整 DEX | 解码层 backlog，低于静态误报 |

仍有效（上轮）：畸形 Manifest 禁止启发式包名 spawn；IPv6/短域名 P0 已在规则里；`_maybe_uninstall` 仍在脱壳 `finally`（hook 已弃，**不必为 hook 去挪**）。

## 3. 代码 / 产物变更要点

- `src/auto_unpack/extraction/indicators.py`
  - `_is_junk_network_token`：无协议 `*.cc`/`*.java`/`Rect.top`/`Filled.*`/`*.cc:行号`、cargo/`termux-android-tools` 路径
  - `url_rank`：灰产 TLD **仅当有 scheme** 才 biz；`host:非 80/443/53` 为 biz；`api.` 前缀也要 scheme
  - noise：`libcore.icu.icu`、`mi1.cc`、`androidx.`
  - `make_indicator` native 降权；`merge_indicators` 保留更高 rank
- `src/auto_unpack/extraction/analyze.py`
  - 跳过 `keyword.txt` / `bad_domains.txt` / `t-rex.html` / emoji xml·json（assets + binary 扫描）
  - `write_url_files`：只写 biz + `_weak_has_signal` 的 weak；页眉「未写入 noise / 裸域名 weak」
- `src/auto_unpack/extraction/net_hook.js`：曾加 DNS/SNI/SSL/X5；**不要接入 pipeline**
- 测试：`tests/test_extraction_indicators.py`、`tests/test_extraction_outputs.py`

行为变化（重跑后才看得见）：

- `https://www.yuliaotongxun.vip`、`39.108.101.79:55007`、`https://8.153.12.23:33926/okhttputil.php` → biz
- `allocation.cc` / `Api.java` → 不是候选
- 裸 `yuliaotongxun.vip` → weak 且不写入文件
- `http://libcore.icu.icu`、`http://s.mi1.cc` → noise

**产物**：`outputs/extracted_urls/` 里 9-07 与 9-08 上午文件都是**改规则之前**的，未重跑。

## 4. 当前状态

- **能用 / 已验证**
  - 生产：`run.py` → 识别壳 → dpt 才 Frida dump → **静态**抽 URL；无壳只静态。厂商壳/自研/VMP 转人工。
  - 规则测试：`pytest tests/test_extraction_indicators.py tests/test_extraction_outputs.py ...` 2026-09-08 全绿。
- **已知限制或坑**
  - 自动化拿不到：加密 native、登录后才下发的接口、Frida 检测包的运行时流量。
  - `https://api.weibo.com/...` 仍可能因 `api.` + scheme 进 biz（SDK 域名表未扩 weibo）。
  - 两标签真 `.cc` 无协议会被当源文件丢掉；有 `http://` 仍可进。
  - 不设 `PYTHONUTF8=1`；项目在 `D:\PythonProject\apk-auto-unpack`。
  - PortableGit：偶发 commit reflog 成功但 ref 不落盘。
- **未完成 / 待办**
  1. **用新规则重跑一批 APK**，看語樂樂 / 特讯达 / HKTV / 学词通的 `urls_by_rank.txt` 是否干净（本会话未重跑）。
  2. 若仍脏：再扩 SDK noise（weibo、netease.im、zego.im、穿山甲）或 so 里 `/netcheck`。
  3. P1 静态解码（`\u0068ttp`、子串 base64）backlog。
  4. 壳识别主线未动：Virbox、复合壳标签（`绘本.apk` 360+dpt+自定义）。
  5. ~~提取层接入 net_hook / 挪 `_maybe_uninstall`~~ **已否决**，除非用户改口。

## 5. 新会话如何继续

按优先级：

1. **重跑静态提取并抽查** 9-08 那批样本（至少語樂樂、特讯达03、HKTV mall、学词通、蓝马集）。
2. 按抽查结果微调 noise/SDK 表，不要再放宽无协议候选。
3. 用户若改回壳识别：Virbox + 复合壳结构化标签。

建议开场（可复制）：

```
请先阅读仓库根目录 交接文档.md，然后继续：用当前静态规则重跑一批 APK（优先語樂樂 / 特讯达03 / HKTV mall / 学词通），抽查 outputs/extracted_urls 的 biz 是否还混着 *.cc 源文件，并只根据抽查结果微调 noise 表。不要接入 net_hook.js。
```

## 6. 关键命令 / 入口

```bash
cd D:\PythonProject\apk-auto-unpack

# 全流程（系统 Python 3.10 + frida，仅 dpt dump 需要设备）
python run.py

# 静态规则测试
C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe -m pytest tests/test_extraction_indicators.py tests/test_extraction_outputs.py -q
```

解释器：`C:/Program Files/Python310/python.exe`（frida + androguard）。
布局：`src/auto_unpack/{flow,runtime,packer,extraction,unpacker}/`；规则 `extraction/indicators.py`，扫描 `extraction/analyze.py`。
`net_hook.js` 不要再作为下一会话主线。
